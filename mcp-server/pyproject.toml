[project]
name = "ways-of-working-mcp"
version = "0.1.0"
description = "MCP server exposing the ways-of-working skills and playbooks to any MCP-capable agent"
requires-python = ">=3.10"
dependencies = ["mcp>=1.2.0,<2", "pyyaml"]
