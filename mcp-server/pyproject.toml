[project]
name = "fable-quality-mcp"
version = "0.1.0"
description = "MCP server exposing the fable-quality-library skills and playbooks to any MCP-capable agent"
requires-python = ">=3.10"
dependencies = ["mcp>=1.2.0"]

[project.scripts]
fable-quality-mcp = "server:main"
