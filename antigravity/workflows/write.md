---
description: Audience → thesis → outline → draft → exactly one structured revision pass. Use for any prose deliverable — docs, READMEs, design docs, proposals, reports, announcements, emails, tutorials — whenever the writing itself is the product rather than a byproduct. `ste-writing` governs sentence style; `plain-language` is the final detection pass. `explain` owns the teaching move.
---

Load and follow the skill at `.agents/skills/write/SKILL.md`. Apply it to the argument given with the command.
