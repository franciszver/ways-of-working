# Files allowed to carry frontmatter keys outside the Agent Skills spec
# set {name, description, license, compatibility, metadata, allowed-tools}.
# One relative path per line, with a comment pointing at the tracking issue.

skills-local/iterate/SKILL.md  # disable-model-invocation, argument-hint — see issue #12
