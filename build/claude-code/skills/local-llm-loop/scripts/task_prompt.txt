In the taskrepo project, Store.get currently returns None for both a missing key and an expired key, giving callers no way to supply a fallback; add a default= parameter to Store.get (in taskrepo/store.py) so it returns that value instead of None when the key is missing or expired, while default=None keeps today's behavior for existing callers, and separately fix taskrepo/serializer.py so that dumps(store) leaves expired entries out of the JSON snapshot entirely (it currently copies every raw entry regardless of expiry) — then update tests/test_store.py and tests/test_serializer.py to assert both new behaviors (get with an explicit default, and an expired key's absence from dumps output) without deleting or weakening any pre-existing assertions, and finish only once python3 -m unittest discover -s tests -t . passes.
