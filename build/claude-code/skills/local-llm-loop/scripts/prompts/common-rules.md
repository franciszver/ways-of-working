## Shared rules (all stages)

- Work in small steps, one file write per turn.
- Run the project's test command after each write, if one exists.
- Never delete files.
- Never run git commands other than `git status` or `git diff`.
